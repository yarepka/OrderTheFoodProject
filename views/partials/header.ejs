<!-- Main nav-bar -->
<div class="line">
  <div class="container">
    <!-- Nav -->
    <nav class="main-nav">
      <a href="/"><i class="logo fas fa-utensils fa-3x"></i></a>


      <ul class="right-menu">
        <% if(login) { %>
        <!-- user logged in -->
        <li>
          <a href="/user/profile">Profile <i class="fas fa-user-circle"></i></a>
        </li>
        <li>
          <a href="/shopping-cart">
            <% if(session.cart) { %>
            <span class="badge"><%= session.cart.totalQty %></span>
            <% } %>
            Cart <i class="fas fa-shopping-cart"></i>
          </a>
        </li>
        <li>
          <a href="/user/logout">Logout <i class="fas fa-sign-out-alt"></i></a>
        </li>
        <% } else { %>
        <!-- user not logged in -->
        <li>
          <a href="/user/signin">Sign In <i class="fas fa-sign-in-alt fa-xs"></i> </a>
        </li>

        <li>
          <a href="/user/signup">Sign Up <i class="fas fa-user-plus fa-xs"></i> </a>
        </li>

        <li>
          <a href="/shopping-cart">
            <% if(session.cart) { %>
            <span class="badge"><%= session.cart.totalQty %></span>
            <% } %>
            Cart <i class="fas fa-shopping-cart"></i>
          </a>
        </li>
        <% } %>
      </ul>

      <div class="menu-wrap">
        <input type="checkbox" class="toggler" />
        <div class="hamburger">
          <div></div>
        </div>
        <div class="menu">
          <div>
            <div>
              <ul class="mobile-menu">
                <% if(login) { %>
                <!-- user logged in -->
                <li>
                  <a href="/user/profile">Profile <i class="fas fa-user-circle"></i></a>
                </li>

                <li>
                  <a href="/shopping-cart">
                    <% if(session.cart) { %>
                    <span class="badge"><%= session.cart.totalQty %></span>
                    <% } %>
                    Cart <i class="fas fa-shopping-cart"></i>
                  </a>
                </li>

                <li>
                  <a href="/user/logout">Logout <i class="fas fa-sign-out-alt"></i></a>
                </li>
                <% } else { %>
                <!-- user not logged in -->
                <li>
                  <a href="/user/signin">Sign In <i class="fas fa-sign-in-alt fa-xs"></i> </a>
                </li>

                <li>
                  <a href="/user/signup">Sign Up <i class="fas fa-user-plus fa-xs"></i> </a>
                </li>

                <li>
                  <a href="/shopping-cart">
                    <% if(session.cart) { %>
                    <span class="badge"><%= session.cart.totalQty %></span>
                    <% } %>
                    Cart <i class="fas fa-shopping-cart"></i>
                  </a>
                </li>
                <% } %>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </nav>
  </div>
</div>